---
type: Fixed
title: Phone HUD fits the screen
---

In portrait the build stamp hangs under the top bar rather than off the
bottom of the screen, and the top bar holds a five-digit stage number and
the clock at every phone width. On iOS the game paints the full screen — the
dead band along the bottom of an installed PWA is gone, while a browser tab
keeps the visible viewport it can actually reach.
