---
type: Added
title: A desktop app for Windows, macOS and Linux
---

Every release now carries the game as a desktop download — an installer for Windows, a `.dmg` for macOS and a `.deb` or `.AppImage` for Linux. It is the same game in a window of its own, offline, that remembers its size and place between launches; F11 or Alt+Enter toggles fullscreen.
